# SVCG Python dependencies
#
# GTK3 bindings are NOT pip-installable on Windows — use MSYS2 MinGW64:
#   pacman -S mingw-w64-x86_64-python-gobject \
#             mingw-w64-x86_64-gtk3 \
#             mingw-w64-x86_64-python-cairo \
#             mingw-w64-x86_64-python-numpy
#
# On Linux (Debian/Ubuntu):
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0 python3-numpy
#   pip install -r requirements.txt
#
# On Linux (Fedora/RHEL):
#   sudo dnf install python3-gobject gtk3 python3-cairo python3-numpy

# Core GTK3 Python bindings (Linux pip install; use MSYS2 on Windows)
PyGObject>=3.42.0
pycairo>=1.20.0

# Numerical / grid operations (A* pathfinding)
numpy>=1.20.0

# Optional — AI backend SDKs (only needed if using cloud AI generation)
# anthropic>=0.25.0
# openai>=1.0.0
